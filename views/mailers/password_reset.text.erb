<%#
  Locals:
    user       - the User requesting the reset
    reset_url  - full URL to the reset-password page, including the token
-%>
Hej<%= " #{user.name}" if user.name %>,

Vi fick en begäran om att återställa lösenordet för ditt Trams-konto.

Välj nytt lösenord: <%= reset_url %>

Länken slutar gälla om 1 timme.

Om det inte var du som bad om detta kan du ignorera det här mejlet.

—
Trams
