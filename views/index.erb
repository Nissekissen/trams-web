<div class="page-header">
  <div>
    <div class="eyebrow">Hem</div>
    <h1>Trams</h1>
  </div>
</div>


<div class="stats-grid">
  <div class="card stat">
    <div class="stat-value"><%= @ride_count %></div>
    <div class="stat-label">Resor</div>
  </div>
  <div class="card stat">
    <div class="stat-value"><%= @ridden_tram_ids.size %>/<%= @total_trams %></div>
    <div class="stat-label">Spårvagnar</div>
  </div>
  <div class="card stat">
    <div class="stat-value"><%= @ridden_lines.size %>/<%= Ride::LINES.size %></div>
    <div class="stat-label">Linjer</div>
  </div>
</div>

<%= erb :'rides/_form' %>

  <div class="card">
    <div class="progress-header">
      <h3>Linjer</h3>
      <span class="muted"><%= @ridden_lines.size %>/<%= Ride::LINES.size %></span>
    </div>
    <div class="line-pills">
      <% Ride::LINES.each do |line| %>
        <% color = Ride.color_for(line) %>
        <div class="line-pill<%= ' line-pill-pending' unless @ridden_lines.include?(line) %>"
             style="background: <%= color[:bg] %>; color: <%= color[:text] %>;">
          <%= line %>
        </div>
      <% end %>
    </div>
  </div>

  <% @models.each do |model| %>
    <% total = model.trams.size %>
    <% next if total.zero? %>
    <% ridden  = model.trams.count { |tram| @ridden_tram_ids.include?(tram.id) } %>
    <% percent = (ridden.to_f / total * 100).round %>
    <div class="card">
      <div class="progress-header">
        <h3><%= h(model.name) %></h3>
        <span class="muted"><%= ridden %>/<%= total %> (<%= percent %>%)</span>
      </div>
      <div class="progress-bar">
        <div class="progress-bar-fill" style="width: <%= percent %>%;"></div>
      </div>
    </div>
  <% end %>
